function clickLogueo(elemento) {
    elemento.innerText = "Logout";
}

function clickDefinicion(elemento) {
    elemento.remove();
}

function clickBotoncito(elemento) {
    alert("Ninja Was Liked");
}